const buttonData = {
  confirm: { prop: 'confirm', label: '确定' },
  cancel: { prop: 'cancel', label: '取消', type: 'info' },
}

const formButtonList = [buttonData.confirm, buttonData.cancel]

export default buttonData

export {
  formButtonList
}
